function testICanSeeUsage()
{
	# Given
	source src/usage
	expect="shtest -h -D [tests] TestSuite -f test"
	
	# When
	actual=$(usage)
	
	# Then
	if [ "${actual}" == "${expect}" ]; then
		exit 0
	else
		exit 1
	fi
}